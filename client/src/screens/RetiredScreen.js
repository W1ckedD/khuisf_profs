import React from 'react';
import { View, Text } from 'react-native';

const RetiredScreen = props => {
    return (
        <View>
            <Text>RetiredScreen</Text>
        </View>
    );
};

export default RetiredScreen;
